%% Please ensure that the image files are in the same root directory as the
% Main script and the subsequent problem scripts.  If the image files are
% not in the same directory, then pass the filename in as the arguement to
% the subsequent functions, as shown in the commented out versions !

imageFile1 = 'F6_30(a)(Vase).tif' ;
imageFile2 = 'Fig0440(a)(testpattern).tif' ;

Problem2() ;

Problem3() ;
%Problem3(imageFile1) ;

Problem4() ; 
%Problem4(imageFile2) ; 