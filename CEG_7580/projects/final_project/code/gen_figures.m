function gen_figures(res)
    %% function to display all processing results
    %
    % in
    %
    % res -> data structure containing all the image results

    fig = figure();
    set(0, 'CurrentFigure', fig);

end
